apiVersion: v1
kind: Service
metadata:
  name: {{ include "jumper.fullname" . }}
  labels:
    {{- include "jumper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jumper.selectorLabels" . | nindent 4 }}
